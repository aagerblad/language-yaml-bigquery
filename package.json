{
  "name": "yaml-bigquery",
  "version": "0.0.0",
  "description": "An example language grammar package",
  "repository": "https://github.com/user/package-name",
  "keywords": [
    "syntax",
    "highlighting",
    "grammar"
  ],
  "license": "MIT",
  "bugs": "https://github.com/user/package-name/issues",
  "engines": {
    "vscode": ">=0.9.0-pre.1"
  },
  "contributes": {
    "languages": [
      {
        "id": "yaml-bigquery",
        "aliases": [
          "yaml-bigquery"
        ],
        "extensions": [
          ".yaml"
        ]
      }
    ],
    "grammars": [
      {
        "language": "yaml-bigquery",
        "scopeName": "source.yaml-bigquery",
        "path": "./grammars/yaml-bigquery.tmLanguage.json"
      }
    ]
  }
}
